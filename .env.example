PORT=5000
NODE_ENV=development

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=ilc_root
DB_NAME=ilc_db

# JWT
JWT_SECRET=change-this-to-a-long-random-secret-in-production
JWT_EXPIRES_IN=7d

# CORS (React dev server)
# Comma-separated allowed frontend origins (CORS)
CLIENT_URL=http://localhost:5173,https://kayshodhatay.com

# Default admin (created on seed)
ADMIN_EMAIL=admin@indialocalconnect.local
ADMIN_PASSWORD=Admin@123

# SMTP — set SMTP_ENABLED=true when ready (coupon emails to customer + business owner)
SMTP_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="India Local Connect <noreply@indialocalconnect.local>"

# Razorpay payments (use test keys in development)
RAZORPAY_KEY_ID=rzp_test_RJ0LaARPhvPWiP
RAZORPAY_KEY_SECRET=I9qge0cT4IzloGTPtRoQpLAi
